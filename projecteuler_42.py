Knowing that the inverse function of n(n+1)/2 is (Sqrt(1+8n)-1)/2, one has only to check if the word-number comes from an integer index using the reverse triangle formula.

For SKY (55) the index will be (Sqrt(1+8*55)-1)/2 = 10.0, which is integer, so 55 must be a triangle number.
